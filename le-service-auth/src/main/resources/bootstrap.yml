spring:
  application:
    name: le-service-auth
  cloud:
    config:
      uri: http://localhost:9004
      name: le-service-auth
      profile: dev
      label: dev-security1
# keytool -genkeypair -alias loveeat  -keyalg RSA -keypass loveeat  -keystore le.keystore -storepass loveeatkeystore